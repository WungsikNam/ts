//Primitives no string boolean 

let age: number;//변수선언 -> 타입지정
//let age: Number;// 이것과 같이 타입설정할때 대문자로 하면 안됨 그것은 Number객체를 가리키게 된다.
// 오류는 나지 않지만 이렇게 소문자로 해주는 것 이 좋다
//age ='12'// 타입을 이미 숫자로 선언해줬기 때문에 ''을 없애줘야돼 
age =12 

let userName: string;

userName='ws'

let isInstructor:boolean;

isInstructor=true

//let hobbies: null;
//이렇게 선언하고 실행하면 에러가 발생함 
//null 과 undefined는 좀 다르게 표현된다


